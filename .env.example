# Plotforge — never commit real values.

NEXT_PUBLIC_SITE_URL=https://plotforge-mu.vercel.app

# Platform key (same Stripe account you sign in with is fine)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Connect with Stripe — Settings → Connect → client id
STRIPE_CLIENT_ID=

PLOTFORGE_UNLOCK_SECRET=
PLOTFORGE_SESSION_SECRET=
PLOTFORGE_ADMIN_PIN=
CRON_SECRET=
